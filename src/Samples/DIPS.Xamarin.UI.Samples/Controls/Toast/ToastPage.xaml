<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DIPS.Xamarin.UI.Samples.Controls.Toast.ToastPage"
             x:Name="toastPage">

    <ContentPage.Content>

        <StackLayout>
            <Label Text="Hello, World!"
                   VerticalOptions="CenterAndExpand"
                   HorizontalOptions="CenterAndExpand" />
            <Button Text="Show Toast 1"
                    Margin="15,0,15,40"
                    Command="{Binding ShowToastCommand, Source={x:Reference toastPage}}"
                    CommandParameter="Hello, Moon!" />
            <Button Text="Show Toast 2"
                    Margin="15,0,15,40"
                    Command="{Binding ShowToastCommand, Source={x:Reference toastPage}}"
                    CommandParameter="Hello, Mars!" />
            <Button Text="Show Toast 3"
                    Margin="15,0,15,40"
                    Command="{Binding ShowToastCommand2, Source={x:Reference toastPage}}"
                    CommandParameter="Hello, Venus!" />
        </StackLayout>

    </ContentPage.Content>

</ContentPage>