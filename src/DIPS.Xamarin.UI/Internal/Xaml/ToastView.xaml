<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DIPS.Xamarin.UI.Internal.Xaml.ToastView"
             x:Name="toastView"

             xmlns:Tag="clr-namespace:DIPS.Xamarin.UI.Controls.Tag;assembly=DIPS.Xamarin.UI"
             xmlns:Xaml="clr-namespace:DIPS.Xamarin.UI.Internal.Xaml;assembly=DIPS.Xamarin.UI"

             VerticalOptions="Start"
             HorizontalOptions="CenterAndExpand">

    <ContentView.Content>
        <Tag:Tag x:DataType="{x:Type Xaml:ToastView}"
                 BindingContext="{Binding Source={RelativeSource AncestorType={x:Type Xaml:ToastView}}}"

                 Text="{Binding Text}"
                 FontSize="{Binding ToastLayout.FontSize}"
                 FontFamily="{Binding ToastLayout.FontFamily}"
                 LineBreakMode="{Binding ToastLayout.LineBreakMode}"
                 MaxLines="{Binding ToastLayout.MaxLines}"
                 TextColor="{Binding ToastLayout.TextColor}"
                 BackgroundColor="{Binding ToastLayout.BackgroundColor}"
                 CornerRadius="{Binding ToastLayout.CornerRadius}"
                 HasShadow="{Binding ToastLayout.HasShadow}"
                 Padding="{Binding ToastLayout.Padding}" />
    </ContentView.Content>

</ContentView>